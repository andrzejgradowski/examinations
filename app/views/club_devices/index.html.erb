<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <h3 class="center">
        <span class="fa fa-satellite-dish glyp-index-title" aria-hidden="true"></span>
        <span class="index-title hidden-xs"><%= t '.title' %></span>
        <span class="index-title visible-xs"><%= t '.navbar' %></span>
      </h3>
    </div> <!--/col-sm-12 -->
  </div> <!--/row -->
</div> <!--/container -->

<div class="container-fluid">
  <div class="row">
    <div class="col-sm-12">
      <table id="club_devices-datatable" class="hover cell-border" cellspacing="0" width="100%", 
        data-source="<%= club_devices_path(format: :json) %>", 
        data-locale_url="<%= datatables_lang_path(format: :json) %>", 
        data-export_csv_url="<%= export_club_devices_path(format: :csv) %>">
        <thead>
          <tr>
            <th>id</th>
            <th><%= ClubDevice.human_attribute_name(:number) %></th>
            <th><%= ClubDevice.human_attribute_name(:valid_to) %></th>
            <th><%= ClubDevice.human_attribute_name(:call_sign) %></th>
            <th><%= ClubDevice.human_attribute_name(:category) %></th>
            <th><%= ClubDevice.human_attribute_name(:transmitter_power) %></th>
            <th><%= ClubDevice.human_attribute_name(:name_type_station) %></th>
            <th><%= ClubDevice.human_attribute_name(:emission) %></th>
            <th><%= ClubDevice.human_attribute_name(:input_frequency) %></th>
            <th><%= ClubDevice.human_attribute_name(:output_frequency) %></th>
            <th><%= ClubDevice.human_attribute_name(:station) %></th>
            <th><%= ClubDevice.human_attribute_name(:applicant) %></th>
          </tr>
        </thead>

        <tbody>
           <!--data from JSON -->
        </tbody>
      </table>
    </div> <!--/col-sm-12 -->
  </div> <!--/row -->

<!-- 
  <div class="row">
    <div class="col-sm-12">

      <%= link_to export_club_devices_path(format: "csv"), class: "btn btn-primary" do %>
        <span class="fa fa-download"></span>&nbsp;<%= t("helpers.links.download") %>
      <% end %>

    </div>
  </div>
-->

</div> <!--/container -->

