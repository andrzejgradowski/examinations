function status_all_is_ok(status_checkbox) {

  if (status_checkbox) {
    document.getElementById("btn_proposal_wizard_finish").disabled = false;
  } else {
    document.getElementById("btn_proposal_wizard_finish").disabled = true;
  };

};